export interface SectionCard {
  titleContent: {
    title: string,
    size: string,
  }
  descriptionContent: {
    description: string,
    icon?: string
    iconPosition?: string,
    image?: string,
    imagePosition?: string,
  }
}
